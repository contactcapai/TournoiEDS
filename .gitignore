# Dependencies
node_modules/

# Environment (les patterns sans slash matchent a tout niveau, ex. apps/*/.env)
.env
.env.local
.env.prod
docker/.env
# Secrets Supabase self-hosted (Story 1.8)
docker/supabase/.env
# Gabarits (.env.*.example) conserves — ils ne contiennent pas de secrets reels
# apps/vitrine/.env.prod.example est versionne (gabarit)
# apps/vitrine/.env.prod est gitignore (secret VPS)

# Build outputs (generiques monorepo : dist/ et .next/ a tout niveau)
dist/
.next/

# Turborepo
.turbo/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Prisma
apps/tournoi-api/prisma/*.db

# Logs
*.log
npm-debug.log*

# Outils dev (Claude Code, Antigravity, BMad skills) — inutile/sensible sur remote
.claude/
.agent/
_bmad/
